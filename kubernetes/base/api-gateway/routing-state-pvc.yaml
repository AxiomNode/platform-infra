apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-gateway-routing-state
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: axiomnode
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi