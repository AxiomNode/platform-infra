apiVersion: v1
kind: Service
metadata:
  name: quizz-db
  labels:
    app.kubernetes.io/name: quizz-db
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: axiomnode
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: quizz-db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
