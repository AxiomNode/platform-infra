apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-wordpass-api
  labels:
    app.kubernetes.io/name: microservice-wordpass-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: axiomnode
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: microservice-wordpass-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: microservice-wordpass-api
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: axiomnode
    spec:
      containers:
        - name: microservice-wordpass
          image: ghcr.io/axiomnode/microservice-wordpass-api:latest
          imagePullPolicy: Always
          command: ["sh", "-c"]
          args:
            - "npx prisma migrate deploy && node dist/app/server.js"
          ports:
            - containerPort: 7100
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: AI_ENGINE_BASE_URL
              value: "http://ai-engine-api:8001"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: wordpass-db-secret
                  key: database-url
          envFrom:
            - secretRef:
                name: microservice-wordpass-secret
                optional: true
            - configMapRef:
                name: microservice-wordpass-config
                optional: true
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 7100
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 7100
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
