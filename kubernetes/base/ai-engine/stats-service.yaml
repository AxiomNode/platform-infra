apiVersion: v1
kind: Service
metadata:
  name: ai-engine-stats
  labels:
    app.kubernetes.io/name: ai-engine-stats
    app.kubernetes.io/component: stats
    app.kubernetes.io/part-of: axiomnode
spec:
  selector:
    app.kubernetes.io/name: ai-engine-stats
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
